import { QrResultDirective } from './qr-result.directive';

describe('QrResultDirective', () => {
  it('should create an instance', () => {
    const directive = new QrResultDirective();
    expect(directive).toBeTruthy();
  });
});
